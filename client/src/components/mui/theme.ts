import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      light: "#fad2a4",
      main: "#f28e1c",
      dark: "#61390b",
      contrastText: "#fff",
    },
  },
});

export default theme;